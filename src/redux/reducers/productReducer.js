import { ActionTypes } from "../constants/action-types"
const initialState = {
    products:[]
}
export const productReducer = (state, action) ={
    switch (type) {
        case value:
            
            break;
    
        default:
            break;
    }
    
}